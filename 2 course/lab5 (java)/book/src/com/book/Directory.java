package com.book;

public class Directory extends Book {
    public Directory(String cipher, String author, String name, String year, String publisher) {
        super(cipher, author, name, year, publisher);
    }
}
